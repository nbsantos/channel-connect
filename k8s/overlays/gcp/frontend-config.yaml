apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: channel-connect-frontend
  namespace: channel-connect
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT
